// 데모 배포본 스위치.
//
// ⚠ 기본값은 반드시 꺼짐(OFF)이다.
//   이 저장소는 다른 교회가 그대로 복사해 실제 운영에 쓰는 템플릿 본체다.
//   데모 기능(매일 새벽 4시 데이터 초기화 / 로그인 화면 체험 계정 안내)이
//   실사용 배포본에서 켜지면 실제 참가자 데이터가 매일 지워지는 사고가 난다.
//
//   켜는 방법: 데모 배포본의 .env.local 에만 `VITE_DEMO_MODE=true` 를 넣는다.
//   (.env.local 은 .gitignore 의 `*.local` 로 커밋되지 않는다 = 복사해 간 교회에는 따라가지 않는다.)
//
//   'true' 문자열과 정확히 일치할 때만 켜진다. 미설정·빈 값·'false'·'1'·'TRUE' 는 전부 꺼짐이다.
//   빌드 시점에 상수로 접히도록 일부러 단순 비교만 쓴다(꺼진 빌드에서는 데모 코드가 통째로 제거된다).

export const DEMO_MODE: boolean = import.meta.env.VITE_DEMO_MODE === 'true';
